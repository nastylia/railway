<%= form_for [:admin, train, carriage] do |f| %>
  <% if carriage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carriage.errors.count, "error") %> prohibited this cariage from being saved:</h2>

      <ul>
      <% carriage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %>
    <%= f.select :type, carriage.get_all_types_values %>
  </div>

  <div class="field">
    <%= f.label :up_seats%>
    <%= f.number_field :up_seats %>
  </div>

  <div class="field">
    <%= f.label :down_seats %>
    <%= f.number_field :down_seats %>
  </div>

  <div class="field">
    <%= f.label :side_up_seats%>
    <%= f.number_field :side_up_seats %>
  </div>

  <div class="field">
    <%= f.label :side_down_seats %>
    <%= f.number_field :side_down_seats %>
  </div>

  <div class="field">
    <%= f.label :sit_seats %>
    <%= f.number_field :sit_seats %>
  </div>


  <div class="field">
    <%= f.label :train %>
    <%= f.collection_select :train_id, Train.all, :id, :number %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
